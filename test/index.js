var should = require('chai').should(),
    nvec = require('../index');

describe('#equals', function() {

    it('verifies date for 2016', function() {
        console.log(JSON.stringify(nvec, null, 2));
        //nvec.BugsDates.easterSunday.should.equal(new Date(2016, 2, 28));
    });

  });
